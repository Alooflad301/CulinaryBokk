<Page x:Class="CulinaryBokk.PageTask"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CulinaryBokk"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageTask">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="0">
            <ComboBox x:Name="ComboFilter" Height="26" Width="289" SelectionChanged="ComboFilter_SelectionChanged"/>
            <ComboBox x:Name="ComdoSort"  Margin="5" VerticalAlignment="Center" Width="190" Height="26" SelectionChanged="ComdoSort_SelectionChanged"/>
            <TextBox x:Name="TextSearch" Margin="5" TextWrapping="Wrap"  VerticalAlignment="Center" Width="186" Height="26" TextChanged="TextSearch_TextChanged"/>
        </StackPanel>
        <ListView Grid.Row="1" x:Name="listProduct" Margin="0,8,0,15" MouseDoubleClick="listProduct_MouseDoubleClick">
            <ListView.Background>
                <RadialGradientBrush>
                    <GradientStop Color="#FFCACACA" Offset="0"/>
                    <GradientStop Color="#FFA0A0A0" Offset="1"/>
                </RadialGradientBrush>
            </ListView.Background>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="400"/>
                                <ColumnDefinition Width="200"/>
                                <ColumnDefinition Width="160"/>
                            </Grid.ColumnDefinitions>
                            <Border
                                Width="200"
                                Height="200"
                                Margin="3"
                                ClipToBounds="True"
                                Grid.Column="0">
                                <Image Source="{Binding CurrentPhoto}" Stretch="UniformToFill"/>
                            </Border>
                            <StackPanel  Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Left">
                                <TextBlock  Height="auto" Text="{Binding RecipeName}"/>
                                <TextBlock TextWrapping="Wrap" Height="auto" Margin="0,0,0,20">
                                <Run Text="Категория: "/>
                                 <Run Text="{Binding Categories.CategoryName}"/>

                                </TextBlock>
                                <TextBlock TextWrapping="Wrap"  Height="auto" Margin="0,0,0,80">
                                        <Run Text="Описание: "/>
                                        <Run Text="{Binding Description}"/>

                                </TextBlock>
                                <TextBlock TextWrapping="Wrap"  Height="auto" Margin="0,0,0,10">
                                        <Run Text="Автор: "/>
                                        <Run Text="{Binding Authors.AuthorName}"/>

                                </TextBlock>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center">
                                <TextBlock TextWrapping="Wrap"  Height="auto" Margin="0,0,0,10">
                                                <Run Text="Время готовки: "/>
                                                <Run Text="{Binding CookingTime}"/>
                                                <Run Text=" минут"/>
                                </TextBlock>

                            </StackPanel>
                            <Button Grid.Column="3" Content="Добавить"  VerticalAlignment="Center" Margin="5" Height="40" Width="100" Background="#FF4CAF50"  FontSize="14" Cursor="Hand" Foreground="White" />
                        </Grid>
                    </Border>

                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <TextBlock x:Name="tbCounter" HorizontalAlignment="Left" Margin="10,0,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Center" Grid.Row="2" Foreground="White"/>
        <Button Content="Добавить рецепт" HorizontalAlignment="Left" Margin="599,0,0,0" Grid.Row="2" VerticalAlignment="Center" Height="42" Width="163" Background="White" FontFamily="Segoe Script" FontSize="16" Click="Button_Click"/>


        <Grid.Background>
            <RadialGradientBrush>
                <GradientStop Color="#FFD41891" Offset="0"/>
                <GradientStop Color="#FF421A56" Offset="1"/>
                <GradientStop Color="#FFAF0000" Offset="2"/>
                <GradientStop Color="#FFC11C02" Offset="2"/>
                <GradientStop Color="#FFFF0404" Offset="4"/>
            </RadialGradientBrush>
        </Grid.Background>
    </Grid>
</Page>
